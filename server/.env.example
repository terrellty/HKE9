# Render PostgreSQL External URL
# Set this exact value in Render service Environment Variables (do not commit real secrets)
DATABASE_URL=postgresql://admin:REPLACE_WITH_PASSWORD@dpg-d65quvn5r7bs73bkvtpg-a.singapore-postgres.render.com/game_record

# Render Postgres usually requires SSL
PGSSLMODE=require

# Optional: server port (Render injects PORT automatically)
PORT=3000
